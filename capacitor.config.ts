import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'domicilios-geograficos_v2.0',
  webDir: 'www'
};

export default config;
